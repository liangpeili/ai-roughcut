# AGENTS.md

## 目标

AI Roughcut 是纪实访谈、街采、口述访谈和课程访谈的自动粗剪工具。项目只处理机械环节：统一素材格式、转写、检测长空白、标出口头禅候选、生成 AI 判断任务、合并剪辑清单、输出粗剪视频、字幕、复查报告和剪映/CapCut 交接包。

本项目不负责最终内容取舍和包装。最终节奏、字幕样式、BGM、花字、转场、调色和发布版本在剪映中完成。

## 目录

- `input/`: 用户放入待处理原始视频。不要移动或删除用户原始素材。
- `work/<素材名>/`: 单条素材的中间文件，包括规范化视频、音频、转写、候选、AI 任务、AI 结果、剪辑清单和临时 clips。
- `output/<素材名>/`: 单条素材的交付文件，包括粗剪视频、字幕、复查报告和 handoff 包。
- `output/<素材名>/handoff/`: 给剪映/CapCut 或人工精修使用的交接文件。
- `src/ai_roughcut/`: 业务代码。
- `tests/`: 纯逻辑和输出格式测试。

## 流程

默认流程：

1. 读取 `input/` 中的视频。
2. 使用 FFmpeg 统一格式，输出 `work/<素材名>/00_normalized.mp4`。
3. 提取音频并用 WhisperX 转写。
4. 检测长空白和口头禅候选。
5. 构造 AI 判断任务；启用 `--ai` 时调用 OpenAI-compatible API，否则使用本地保守规则。
6. 校验 AI 返回结果。缺失、非法 action、非法 confidence、时间不匹配、compress 缺少 `target_duration` 的结果必须转人工复查。
7. 生成 `work/<素材名>/cuts/cut_list.json`。
8. 渲染 `output/<素材名>/rough_cut.mp4` 和 `final_clean.mp4`。
9. 可选生成字幕和烧录字幕版本。
10. 写入 `output/<素材名>/review_report.html` 和 `handoff/` 交接包。

## 决策规则

- 默认自动执行阈值是 `auto_confidence = 0.75`。
- 默认人工复查阈值是 `review_confidence = 0.60`。
- 0.8 秒以内停顿忽略。
- 0.8-1.5 秒停顿只标记。
- 1.5 秒以上停顿建议压缩。
- 3 秒以上停顿压缩后默认保留 0.6 秒。
- 删除口头禅等片段时可以使用小幅 `cut_margin`。
- 压缩长空白时只删除空白尾段，不向空白外扩展，避免误伤下一句话开头。
- 不确定的候选必须进入 review，不要硬删。

## 交接输出

`output/<素材名>/handoff/` 必须包含：

- `timeline.fcpxml`: 单轨时间线交换文件，优先尝试导入支持 XML/FCPXML 的剪映或 CapCut 桌面版。
- `edit_decisions.csv`: 保留和删除片段的源时间码、时间线时间码和原因。
- `roughcut_recipe.json`: 机器可读剪辑配方，记录源片段、时间线片段、删除片段和复查标记。
- `import_notes.md`: 导入建议和 XML 不兼容时的兜底流程。

## 人工审核标准

粗剪输出后必须提醒用户检查：

- 句首或句尾有没有被吃掉。
- 长空白压缩后是否仍保留人物思考和情绪。
- 被访者表达是否完整，语义有没有被误删。
- 低置信度、AI 返回异常或敏感内容是否需要手动调整。
- 字幕、声音和画面是否对齐。
- 剪映/CapCut 是否能打开 XML 时间线；如果不能，使用 `rough_cut.mp4` 或 `final_clean.mp4` 兜底。

## 禁止事项

- 不要移动、删除或覆盖 `input/` 中的原始素材。
- 不要为了减少人工复查而自动执行低置信度删除。
- 不要让 AI 自行合并、扩展或新造候选时间段。
- 不要在没有测试的情况下修改剪辑决策逻辑。
- 不要把剪映原生草稿格式当成稳定公开接口；除非用户提供样例草稿并明确要求实验。

## 验证

修改代码后至少运行：

```bash
python -m pytest
```

涉及剪辑决策、AI 结果、handoff 输出或报告格式的修改，必须补充或更新对应测试。
